"""Scripts that reproduce each figure in the DDMC paper, plus `common.py`'s
shared plotting helpers. Each `figureM*.py` module exposes a `makeFigure()`
function, invoked by the `fbuild` console script (`ddmc.figures.common.genFigure`).
"""
